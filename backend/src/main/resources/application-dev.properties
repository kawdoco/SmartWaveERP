# Development Profile Configuration

# Enable detailed error messages in development
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always
server.error.include-exception=true

# H2 Console (optional - for local testing without Supabase)
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# Database Configuration (Supabase PostgreSQL via PgBouncer Pooler)
# Note: Spring Boot JDBC requires 'jdbc:postgresql://' prefix. 
# Also, '?prepareThreshold=0' is required when using PgBouncer in Transaction mode (default Supabase pooled mode)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.wcbtapepxvvyikqekejb
spring.datasource.password=23@Ttnt4532
spring.datasource.driver-class-name=org.postgresql.Driver

# For DDL (table creations/migrations), some operations might fail through the pooler.
# If hibernate.ddl-auto=update fails, uncomment and use the DIRECT connection below instead:
# spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres

# Development logging
logging.level.org.springframework.security=DEBUG
